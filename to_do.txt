1. User Profile Pages
 display user info
 display all posts by that user
 display who that user is following
 button to follow user -- ONLY if session id != user_id
 button to edit -- ONLY if session_id == user_id
 button to post joke -- ONLY if session_id == user_id

 + need route that captures user id number and creates page
 + need Users page to link to profiles


2. Follows


3. Modals


4. Hide Signout button when there's no session

5. Post A Joke - add conditional that redirects to homepage if user is not signed in -- use a flash message?

6. Add character limit to post


7. Styling

- make delete button larger + red/highlighted
- make fonts/font sizes/margins consistent
- feed styling.  border?